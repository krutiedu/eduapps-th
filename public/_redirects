# Cloudflare Pages — เสิร์ฟ index.html ให้เส้นทางของ SPA ที่ยังไม่มีหน้า SSR เฉพาะตัว
# (200 = rewrite คือคง URL เดิมไว้ ไม่ใช่ redirect)
#
# เส้นทางที่มี Function ของตัวเองอยู่แล้วไม่ต้องใส่ที่นี่:
#   /apps/:id  /worksheet/:id  /article/:id  /sitemap.xml
#
# เส้นทางที่ไม่อยู่ในลิสต์นี้และไม่มีไฟล์จริง จะตกไปที่ 404.html ตามปกติ

/apps       /index.html   200
/worksheets /index.html   200
/blog       /index.html   200
/about      /index.html   200
/buy        /index.html   200
/report     /index.html   200
/privacy    /index.html   200
